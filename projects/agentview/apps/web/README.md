# AgentView Frontend

This directory is a minimal static frontend for GitHub Pages.

It now includes a co-host panel that talks to the API proxy for Looki data.

## Deploy target

- GitHub Pages serves the contents of this directory.

## Local preview

Open `index.html` directly or serve the directory with any static file server.
