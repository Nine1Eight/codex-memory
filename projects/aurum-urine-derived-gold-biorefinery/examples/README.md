# Example-data status

`reference_scenario.json` is a **hypothesis fixture**, not an experimental result. Its values
exercise every stage of the model, including an affinity/capacity bottleneck and capacity loss
across secretome reuse. The scenario must not be cited as evidence of feasibility, yield, purity,
cost, or commercial performance.

Replace individual assumptions with independently measured values and change
`evidence_status` only after satisfying the gates in `docs/VALIDATION_PLAN.md`.

